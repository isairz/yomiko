/** @jsx React.DOM */
var React = require('react');

var Viewer = React.createClass({
  render: function() {
  	return (
      <div>
        Viewer is not implemnted yet.
      </div>
  	);
  }
});

module.exports = Viewer;
